# Local development stack. Run from repo root: `docker compose up`

services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: foodlab
    ports:
      - "5432:5432"
    volumes:
      - foodlab_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 10

  # Bundled Ollama is opt-in. By default the api talks to a native Ollama
  # process on the host (much faster on macOS thanks to Metal GPU acceleration).
  # To run the bundled CPU-only Ollama instead:
  #   docker compose --profile bundled-ollama up -d
  ollama:
    profiles: ["bundled-ollama"]
    build:
      context: .
      dockerfile: ai/Dockerfile
    environment:
      OLLAMA_MODEL: ${OLLAMA_MODEL:-qwen3.5:4b}
    ports:
      - "11434:11434"
    volumes:
      - foodlab_ollama:/root/.ollama
    healthcheck:
      # The ollama image ships no curl — probe via the CLI instead.
      test: ["CMD-SHELL", "ollama list >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/foodlab
      # LLM defaults to Google Gemini — pass LLM_API_KEY through from your
      # shell / .env. To use the bundled CPU Ollama instead, set
      # LLM_BASE_URL=http://ollama:11434/v1 and LLM_MODEL=qwen3.5:4b.
      LLM_API_KEY: ${LLM_API_KEY:-}
      LLM_BASE_URL: ${LLM_BASE_URL:-https://generativelanguage.googleapis.com/v1beta/openai}
      LLM_MODEL: ${LLM_MODEL:-gemini-3.1-flash-lite}
      JWT_SECRET: ${JWT_SECRET:-dev-insecure-secret-change-me}
      API_PORT: 3001
      RECIPES_DIR: /recipes
    extra_hosts:
      # Lets the container resolve the host on Linux too (no-op on Docker Desktop).
      - "host.docker.internal:host-gateway"
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "3001:3001"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3001/api/health >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: app/Dockerfile
    environment:
      API_UPSTREAM: http://api:3001
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "8080:8080"

volumes:
  foodlab_pg:
  foodlab_ollama:
